defaults:
 - general
 - wandb
 - _self_
 
rx:
  mode: train_apply # Options: train_apply, per_element
data:
  train:
    cu3s_path: ???
    label_path: null
    mes_ids: ??? # List of measurement IDs for training
  val:
    cu3s_path: ${data.train.cu3s_path}
    label_path: ${data.train.label_path}
    mes_ids: ??? # List of measurement IDs for validation
