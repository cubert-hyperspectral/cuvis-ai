<!--
  This page is generated at build time by scripts/generate_node_catalog.py
  via the mkdocs-gen-files plugin. The rendered content (filter bar +
  collapsible per-node rows) lives in the build output, not in this file.
  Editing here has no effect — change the generator or the underlying
  Node._category / _tags / docstrings instead.
-->

# Nodes Catalog
