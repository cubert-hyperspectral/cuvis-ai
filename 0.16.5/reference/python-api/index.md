# Python API

Framework-level API documentation generated from current docstrings. Node-level docstrings live in the [Nodes catalog](https://docs.cuvis.ai/latest/catalogs/nodes/index.md); this section is the reference for the framework that hosts the nodes.

## API modules

- **[Pipeline & Graph](https://docs.cuvis.ai/latest/reference/python-api/pipeline/index.md)**

  ______________________________________________________________________

  Pipeline and graph construction APIs.

- **[Ports](https://docs.cuvis.ai/latest/reference/python-api/ports/index.md)**

  ______________________________________________________________________

  Port system and data contracts.

- **[Utilities](https://docs.cuvis.ai/latest/reference/python-api/utilities/index.md)**

  ______________________________________________________________________

  Helper functions and utilities.

- **[gRPC API](https://docs.cuvis.ai/latest/deployment/api/session/index.md)**

  ______________________________________________________________________

  The remote CuvisAIService API surface. See also [Deployment](https://docs.cuvis.ai/latest/deployment/index.md).

## Node API

Node implementations are documented in the [Nodes catalog](https://docs.cuvis.ai/latest/catalogs/nodes/index.md), which renders every built-in and plugin node with its category, tags, and live docstrings — filter by `category` or `tag` to narrow the list (e.g. `#category=model`).
